## v0.2.19 更新内容

### 战绩显示 - 恢复轻量版插件
- 移除 Sona 完整 bundle（index.css/assets/），不再注入 Sona UI 面板
- 恢复轻量版插件，只注入战绩数据（胜率/KDA/评分），不显示额外 UI

### 关键修复
- **添加 2 秒轮询机制**：不再仅依赖 socket 事件，即使 `context.socket` 不可用也能正常工作
- **添加 `metadata.tags` 过滤**：与 Sona 一致，支持通过 tags 过滤队列
- **大厅变化签名检测**：通过 JSON 签名检测大厅成员变化，避免不必要的重复查询
- **更详细的日志**：每个关键步骤都有 `[ARAMGG]` 前缀日志，方便在 DevTools 控制台排查

### 使用说明
1. 下载并安装 `aramgg_client Setup 0.2.19.exe`
2. 打开 ARAMGG 客户端，进入"客户端战绩注入"面板
3. 点击"安装 Pengu Loader"（程序自带，无需下载）
4. 点击"安装插件"
5. 刷新确认安装状态为"已安装"
6. 重启 LoL 客户端
7. 大厅战绩和选人阶段战绩默认开启
8. 如果不显示，按 F12 打开 DevTools 控制台，搜索 `[ARAMGG]` 查看日志
